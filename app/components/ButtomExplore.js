import React from "react";
import { Text, StyleSheet,TouchableOpacity ,ImageBackground} from "react-native";
import {ICONS,LAYOUT} from '../modules';
export default (ButtomExplore = () => {
  return (
    <TouchableOpacity>
          <Text style={styles.textLabe}>IELTS Preparation</Text>
    </TouchableOpacity>
  );
});

const styles = StyleSheet.create({
    
});
